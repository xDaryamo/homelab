apiVersion: v1
kind: ConfigMap
metadata:
  name: glance-config
  namespace: glance
data:
  glance.yml: |
    theme:
      background-color: 240, 0%, 94%
      primary-color: 30 30 30
      text-color: 30 30 30
    pages:
      - name: Home
        columns:
          - size: full
            widgets:
              - type: clock
