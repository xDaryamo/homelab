apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: subito-searcher-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn-ssd
