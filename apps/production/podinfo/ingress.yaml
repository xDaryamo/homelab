apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: podinfo
  namespace: podinfo
spec:
  values:
    ingress:
      enabled: true
      className: nginx
      hosts:
        - host: podinfo.dariomazza.net
          paths:
            - path: /
              pathType: ImplementationSpecific
